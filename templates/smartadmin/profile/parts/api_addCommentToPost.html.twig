<div>
    <div class="d-flex flex-row w-100 pt-4 slide-down" style="display: none">
        <div class="d-inline-block align-middle status status-sm status-success mr-3">
            <span class="profile-image profile-image-md rounded-circle d-block mt-1" style="background-image:url('{{ asset('upload/avatars/'~Author.Username~'/'~Author.AvatarFileName)|imagine_filter('my_thumb') }}'); background-size: cover;"></span>
        </div>
        <div class="mb-0 flex-1 text-dark">
            <div class="d-flex">
                <a href="javascript:void(0);" class="text-dark fw-500">
                    {{ Author.Username }}
                </a>
                <span class="text-muted fs-xs opacity-70 ml-auto">
                    {{ CreatedAt|ago }}
                </span>
            </div>
            <p class="mb-0">
                {{ Content }}
            </p>
            <div>
                <a href="javascript: addHeart('{{ commentId }}');" class="d-inline-flex float-right small align-items-center text-dark">
                    <i class="fas fa-heart fs-xs mr-1 text-danger"></i> <span>0 Likes</span>
                </a>
                <a href="#answer_{{ commentId }}" data-toggle="collapse" aria-expanded="false" aria-controls="answer_{{ commentId }}" class="float-left">
                    <span class="small" data-toggle="tooltip" data-original-title="Create new conversation" data-placement="top">
                        <i class="far fa-caret-down"></i> Answer </span>
                </a>
            </div>
            <div id="conversation_target_{{ commentId }}_after"></div>
        </div>
    </div>
    <!-- add comment -->
    <div class="py-3 w-100 collapse" id="answer_{{ commentId }}">
        <form id="COMMENTinconversation_{{ commentId }}" method="post">
                                              <textarea id="new_comment_to_the_conversation_{{ commentId }}"
                                                        class="form-control border-0 p-0" name="Comment_content"
                                                        placeholder="add a comment..."
                                                        rows="2" style="background-color: unset"
                                                        onkeypress="submitCommentToComment('comment', '{{ commentId }}', '1')"></textarea>
            <input type="hidden" name="_token" value="{{ csrf_token('comment') }}">
            <input type="submit"
                   style="position: absolute; left: -9999px; width: 1px; height: 1px;"
                   tabindex="-1"/>
        </form>
    </div>
    <!-- add comment end -->
</div>